// A mini parking system with lots of 20 slots, a car can enter if there is free slot from right to left(slot1 - 20) 
//a car only leave slot by speciafying the slot number where it parked and should be able to display parking status 
//after each action of available space and filled space.
console.log("WELCOME TO CAR SLOT");
let parkingLot = new Array(20);

function enterCar() {
   
    for (let index = 0; index < parkingLot.length; index++) {
        if (parkingLot[i] = 1) {
            console.log(`Car parked in slot ${i + 1}`);
        }
    }
}

